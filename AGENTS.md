# 项目协作规则

## 范围与入口

本文件适用于当前 Git 仓库及其子目录。先确认工作目录：本机仓库为 `D:\carrie\recommend-demo\ai-recommend-demo`；父目录另有同名代码，不要混用或同步修改。

按任务需要阅读，不必每次扫描全仓库：

- [README](README.md)：运行、配置、验证、部署及 Demo 限制。
- [V2 架构合同](docs/V2_ARCHITECTURE_CONTRACT.md)：当前业务职责与数据合同。
- [V2 评估参数](docs/V2_EVALUATION_PARAMETERS.md)：参数及确认状态，实验值不等于生产规则。
- [Debug / Trace](docs/V2_DEBUG_TRACE.md)：调试视图的使用与证据边界。
- [历史验收报告](docs/V2_ACCEPTANCE_REPORT.md)：仅证明报告记录版本的结果。

当前用户要求决定任务范围。若需求与架构合同存在实质冲突，指出冲突并澄清；不要静默改变业务规则。V1 规范及旧客户指南仅作历史参考。子目录 AGENTS.md 补充对应目录的专用规则。

## 工作方式

- 默认用中文沟通。简单、可读、小步修改；不做无关重构或无依据的抽象。
- 先检查 `git status --short` 及相关 diff，保留已有未提交改动；不能把已有改动全部归为本次成果。
- 先理解目标、影响范围和可验证的完成标准，再实施。小任务不必输出完整需求、架构和 AI 分析文档。
- 已授权的实现或修复直接推进；不以“等待确认”为固定阶段。关键需求缺失或行动超出已有授权时，说明原因并询问。
- 区分事实、假设和未验证结论；优先查明根因，不能用静默降级掩盖失败。
- 新增依赖、服务或框架前检查现有能力。仅阅读相关文件，保留有效发现，避免重复探索。

## 业务边界

详细规则以 V2 架构合同为准；以下原则不得因模型或开发工具变化而丢失：

- Model 负责语言理解、结构化意图、定性策略、优先级与推荐理由。
- Workflow 负责可信身份、候选过滤、预算、库存、数量、价格、包装单位、计算和校验。模型不得生成可执行数量或绕过硬约束。
- 校验器使用可信主数据重新计算，不能仅相信模型或 Optimizer 自报值。
- V2 是主流程；V1 降级保留原始失败原因及未验证标识。合法空候选应返回不采购，不应触发降级。
- 未知数据与零值不同；涉及缺失库存、无效预算等情况时，保持显式状态并验证相应行为。
- 修改 Prompt、Schema、数据或参数时检查上下游合同和回归风险，同步必要文档。

## 委派与模型

主代理负责需求、架构、复杂调试、相关 diff 审查和最终验收。仅在子任务边界清晰、能够独立验证且协调成本有收益时使用原生子代理；极小任务直接完成。

子任务按以下策略显式选择模型；不改变当前主代理模型：

| 子任务 | 模型 | 推理强度 |
| --- | --- | --- |
| 明确、重复、局部修改；按既定规格补测试或整理文档 | `gpt-5.6-luna` | `medium` |
| 常规代码探索、局部实现、回归分析（默认） | `gpt-5.6-terra` | `medium`；复杂分析用 `high` |
| 跨模块正确性、业务约束审查、复杂调试 | `gpt-5.6-sol` | `high` |
| 难以定位的问题、重大架构判断 | `gpt-6-astra` | `high`；通常由主代理直接承担 |

不默认使用 `max` 或 `ultra`。任务复杂度超出当前模型适用范围时，先返回证据并重新划分任务，必要时升级模型；不要只提高推理强度或反复重试。上述分配是项目执行策略，尚未通过本项目模型对照实验验证成本或质量优势。

委派时提供目标、相关文件、接口、限制和验收标准，优先传递精简上下文；选择不同模型时使用当前工具支持的模型覆盖方式。模型不可用时说明情况，使用当前可用能力完成或重新安排，不静默声称已使用指定模型。子代理遇到歧义或超范围问题应返回证据，最终验收仍由主代理负责。

单纯运行测试通常直接调用工具；只有存在可独立开展的失败分析、测试设计或其他有价值工作时才委派。

开发委派不得改变产品的 Workflow / Model 分工。不要为此修改产品 provider adapter、创建自定义模型 router、调用额外 API 或引入 Agent Runtime；不要自行修改 Codex 配置。

## 验证与完成标准

在仓库根目录运行，首次准备环境见 README。

```powershell
# 离线回归；当前 PowerShell 会话内禁用真实模型
$env:AI_ENABLED = 'false'
.\.venv\Scripts\python.exe -m unittest discover -s tests
```

- 行为修复补能复现缺陷的回归测试，验证正常路径、相关边界及降级路径；跨模块改动运行完整离线测试。
- 纯文档改动检查本地链接、命令与代码是否一致、编码和 diff；不为此机械重跑全部业务测试。
- 真实模型测试与离线测试分开。只在任务授权包含相应出站数据及调用时执行；记录实际 provider/model、代码、数据、Prompt、参数、失败与降级情况。
- 代码、模型、Prompt、数据或参数改变后，按影响范围重新验收；不能沿用历史报告宣称当前版本已通过。UI 行为需验证实际交互或明确未验证。
- 不打印或提交 `.env`、密钥、完整敏感响应；对日志和 Trace 的脱敏同样验证。
- 完成时说明改了什么、为什么、验证结果及未验证项。不自行提交、推送或部署，除非任务已授权。
- 完成长效指令更新后先报告结果，再继续其他开发任务。

## UI 演示视频

源截图视为母版素材，不得为合成渲染降低母版分辨率。模糊背景必须使用复制的背景图层，不能模糊前景 UI 图层。进入视频子目录时先阅读其专用规则。
